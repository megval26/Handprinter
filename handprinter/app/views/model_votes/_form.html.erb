<%= form_for(@model_vote) do |f| %>
  <% if @model_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@model_vote.errors.count, "error") %> prohibited this model_vote from being saved:</h2>

      <ul>
      <% @model_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :model_id %><br>
    <%= f.number_field :model_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
